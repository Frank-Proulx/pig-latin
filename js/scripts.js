// Business Logic

function pigLatin(word, text) {
  if (text.trim().length === 0){
    return 0;
  }
  let wordVowels = 0;
  const wordArray = text.split(" ");
}

function pigLatin(letter) {
  return letter + "way";
}